monolog:
    handlers:
        blueScreen:
            type: tracyBlueScreen
            level: critical
            path: %kernel.logs_dir%
            bubble: false
            channels: ["channel"]
